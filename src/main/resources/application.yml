server:
  port: 8080
  compression:
    enabled: true
  address: 0.0.0.0

logging:
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  level:
    root: INFO
    org.springframework: DEBUG
    com.ic.sandbox: DEBUG